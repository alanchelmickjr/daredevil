B1 calibration fit: held-out H1 split (enroll first 10s, score last 10s); world phase samples OTHER VOICES default-on; floor impostor to textbook 0.18+-0.11 via max(); label H0 source in calibration.json; refuse d' on defaulted H0. CHECK: impostor_mean>=0.15 or H0 labeled measured-voices; 60s non-enrolled voice -> zero MATCHED lines (doc path step 2)
M1 hot-apply: EnrollmentManager.apply_model(idm) — set identity, recompute A/B, reseed background, reset_accumulators; call from _do_fit and CLI. CHECK: Wald bounds logged before/after in-HUD fit change without restart; pin with test (doc path step 3)
B2 revocable hold: two-sided SPRT demotion on sustained negative unfloored evidence; ECAPA sanity gate before decided track inherits frames; wire forget_track; immediate-accept needs 2nd confirming frame before hold. CHECK: TV takes over owner track -> demotes to UNKNOWN <=10s; REPLACE tests/test_matching.py irrevocable-hold pins (doc path step 4)
M3 speech-class normalization: is_speech_class() casefold+substring helper at events-slot boundary; fix router.py:88 + server.py:306. CHECK: live PANNs run, active_speaker == owner name while speaking; router reason owner-speaking fires (doc path step 5)
M4+M11 store hygiene: demo/synthetic serve gets isolated data_dir; is_speech_quality + min voiced frames at enroll; casefold names at store/enroll/match; refuse synthetic writes to reference store. CHECK: bare serve leaves voiceprints mtimes untouched; enrolling silence errors; ALAN resolves to Alan (doc path step 6)
M5+M6 runtime H0 + cohort: censored/quantile background estimator (guard NOT anchored to static seed); cohort engages at len(others)>=1 with impostor blending; tie-break on pre-cap evidence. CHECK: unit tests — bg mean rises on guest-voice stream; 2-speaker cohort exercises; no name flap in dual-decided fixture (doc path step 7)
M2+M12+m8 calibration robustness: try/except -> error phase + watchdog + POST /calibrate/cancel; MicStream.flush() at phase starts; confirm-to-save below d'=1.5; low-d' warning in map. CHECK: deny mic mid-phase -> HUD shows error, live map resumes <=60s; d'=0.8 fit prompts (doc path step 8)
M7+M8+M9+m7+m11 HUD truthfulness: held/coasting identity emitted+rendered dimmed; honest mono layout keyed on map.array.spatial (no fabricated bearings); focus gates transcription per-source or legend fixed; caption timestamps/fade; audio-age+timings in map. CHECK: owner pauses 20s -> named dimmed card, no UNKNOWN twin; mono shows no bearing lines; TV during focus produces no misattributed caption (doc path step 9)
M10+M13+m2+m3+m9+m10 robustness: backend-mismatch warn+skip, fallback watermark; L2-normalize embeddings at slot boundary + drift bound; auto-focus active_speaker + sync wake focus (first observed STT->LLM output); anti-alias resample; DAREDEVIL_KEY mismatch surfaced; wake enrollment. CHECK: bare-env launch warns + no identities; owner transcript in HUD <=5s; wrong key shows N-unreadable (doc path step 10)
M14+M15+m4+m6 overlap quality: down-weight mixture windows; energy-ratio cap AFTER distinctness; one match() per frame-track; ECAPA re-binding on track birth; room-calibrated speech gate. CHECK: scripted owner+TV overlap — owner stays named, TV stays UNKNOWN, owner cosine drop <0.15 vs solo (doc path step 11)
