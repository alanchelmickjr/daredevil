# Session — 2026-07-01 (handoff from alan-digital-core)
**Project:** daredevil — local acoustic awareness (WHO/WHERE/WHAT/HOW)
**Context:** The digital-core engine was installed globally today (hooks, safety gate, rolling index, job harness — see `~/dev/Github/alan-digital-core/chronicle/2026-07-01.md` for the full build log). This is the first tenant session. Goal, in Alan's words: **make daredevil the input to the engine, and make it KNOW voices — on this M2 MacBook, today.** It already knows voices on Thor (Jetson); the M2 is the port target.

## Recon already done (from the core session — verified on this machine)

- **On Apple hardware the whole game is WHO.** macOS beamforms the built-in mic array to one processed mono channel, so WHERE/DOA is off the table by design (`docs/HANDOFF.md`, `docs/MACBOOK_COMPLETION_PLAN.md`). Speaker identification is both the constraint and the stated priority. Do NOT fake bearings from mono.
- **Environment:** conda env `daredevil` EXISTS (`/opt/homebrew/Caskroom/miniforge/base/envs/daredevil`). `whisper-server` is installed at `/opt/homebrew/bin/`; `whisper-cpp` binary not found by that name. `sounddevice` is not importable from base python — use the env.
- **CLI surface** (`daredevil/cli.py`): `demo`, `enroll`, `calibrate`, `listen`, `wake`, `serve` (web HUD), `mcp` (stdio MCP server for Claude), `bench`, `devices`, `version`.
- **Known broken (per docs/MACBOOK_COMPLETION_PLAN.md, June 2026 review):**
  - `audio/capture.py:257` — MicStream hardcodes `channels=1`; matters for USB arrays, not for built-in mono.
  - Calibration output captured but never auto-loaded — SPRT runs on VoxCeleb defaults, not Alan's voice/room.
  - STT→LLM loop half-wired: `/probe` returns `implemented: False`.
- **"Make THIS the input":** daredevil ships an MCP server (`daredevil mcp`, stdio). Wiring it into Claude Code makes the live awareness map (WHO is speaking + transcript) a tool the engine can read — the Voyeur-coding loop, formalized.

## The job to author first (turn-sized, per the harness)

1. Step zero (5 min, per their own plan): `conda activate daredevil && python -m daredevil devices` — record built-in mic `max_input_channels`.
2. Baseline: `python -m pytest -q` (expect ~32 passing on stdlib).
3. Verify real ECAPA loads on M2 (torch CPU/MPS) and computes an embedding — no cloud.
4. Enroll Alan (`daredevil enroll` — he speaks, multi-sample if available). Voiceprint persists to `~/.daredevil/voiceprints/`.
5. Live WHO: `daredevil listen` / `serve --live` — DONE_WHEN candidate: **a live run on this M2 identifies the enrolled speaker "alan" by name in the awareness map, across a process restart.**
6. Auto-load calibration into SPRT (the 🟠 gap) if identity confirm is slow/flaky with defaults.
7. Wire `daredevil mcp` into Claude Code so the map becomes engine input.

## Monitor

`while true; do clear; ~/.claude/scripts/status.sh; sleep 2; done` — run it from this directory and it shows THIS project's jobs/chronicle (scope = git root of cwd).
