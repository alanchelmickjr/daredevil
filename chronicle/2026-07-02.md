
##  — [Analysis] Gap analysis: real-time voice discriminator

**What:** 57-agent verified review (5 readers, adversarial verification, 50/51 claims confirmed) -> docs/GAP_VOICE_DISCRIMINATOR.md (137 lines, 60 file:line citations).
**Blockers (3):** calibration accepts any voice above cos~0.26 as owner (room-tone impostor model); identity hold irrevocable per track (raw=-0.150 held as Alan ~8min); no continuous capture loop (mic only samples inside browser GET, <=40% duty).
**Alan's suspicion CONFIRMED:** active_speaker always None in production (case bug 'speech' vs PANNs 'Speech'); synthetic 'alan' had polluted the real store; STT->LLM never observed working; /probe is a stub.
**Sound foundation:** real ECAPA/PANNs/ConvTasNet on M2, ~0.4s/window, honest SPRT core. 11-step ordered path in the doc, each step ends in an observable check.

## 15:22 — [Prep] voice-discriminator-real job authored

**What:** 11-step job active (steps mirror docs/GAP_VOICE_DISCRIMINATOR.md ordered path, each step embeds its observable CHECK). Context-proof manifest at state/jobs/voice-discriminator-real/manifest.md: fresh-session reading order, non-negotiables, checklist, DONE_WHEN (the doc's 8 acceptance criteria). MAX_ITERS 44.
**Decision:** chronicling cadence — Alan flagged under-chronicling; rules/semantic-logging.md:105-116 already mandates logging at every externalization (file change, decision, step, pre-compaction). No rule change; the gap was obedience. Manifest makes it a step requirement.
**Next:** begin step 1 (B3 capture loop) on Alan's go.

## 15:56 — [Step 1 progress] B3 continuous stream implemented

**What:** viz/stream.py (StreamLoop: reader+inferer threads, ring, pause-clears-ring), pipeline.process_capture() split from listen(), server wires loop in live mode (/awareness = snapshot), Config.hop_seconds added + window_seconds finally read. reset_accumulators() now called on calibration fit (first slice of M1 — a new fit invalidates latched decisions).
**Tests:** tests/test_stream.py (4 scheduler tests, no hardware). Full suite 40 passed + 1 PRE-EXISTING flake: test_full_calibration_flow fails ~2/5 on clean HEAD too (git-stash bisect, 3 runs each) — synthetic latching, root cause = B2, fixed properly at step 4.
**Next:** live headless soak — CHECK needs >=55 windows/60s for 10min.

## 16:14 — [Step 1 DONE / Step 2 code landed]

**Step 1 (B3) CHECK OBSERVED:** 10 consecutive minutes at 118-120 analyzed windows/60s (need >=55), zero crashes, GET /awareness 26ms (was 1252ms). Evidence: scratchpad/serve3.log stream lines 15:57-16:08. viz/stream.py + process_capture split + snapshot /awareness. Popped.
**Step 2 (B1) code landed, CHECK pending live recalibration:** held-out H1 split (CLI+HUD), world phase default-on, impostor floored to textbook 0.18±0.11 via max(), h0_source provenance in calibration.json, d-prime refused on defaulted H0, reset_accumulators on fit.
**Flake killed with data:** test_full_calibration_flow asserted WHICH synthetic speaker wins — measured cross-speaker ECAPA cos 0.95 > self 0.932: a coin flip. Test now asserts enrolled-type surfacing; name discrimination stays pinned in test_matching.py. 5/5 stable, suite 41 green.
**Next:** restart serve (new code), Alan recalibrates on viewscreen WITH TV talking in world phase, then 60s TV-only zero-MATCHED check.

## 18:33 — [Fix] Calibration killed the screen: two mic owners (my design error)

**What:** Stream loop held the mic; calibration opened a second InputStream -> PortAudio -9986 -> phase thread died unhandled (gap M2) -> active:true frozen, awareness paused, dead screen. Evidence: serve4.log traceback (capture.py:349 sd.rec), /calibrate/status stuck phase=voice.
**Fix:** single mic owner — StreamLoop.borrow_start/chunk/end diverts reader chunks to calibration (viz/stream.py); _do_capture live path borrows at hop size; _run_phase wrapped try/except -> phase=error + active:false + live view resumes (M2 slice, pulled forward because it bit). Pause hook removed (reader must keep reading during borrow).
**Tests:** borrow diversion/contiguity/resume + dead-reader timeout added; suite 43 green.

## 18:55 — [Steps 3-5 code landed] hot-apply, revocable hold, speech-class fix

**M1:** EnrollmentManager.apply_model() — swaps Gaussians, recomputes Wald bounds, reseeds CFAR, resets accumulators; called from HUD _do_fit. Test pins bound recompute.
**B2:** revocable hold — post-decision contrary evidence pools UNFLOORED; at B*2.0 the identity revokes (SPRT revoke log line). Plus: per-frame LLR clipped to 0.9*A (NO single frame can decide alone — blip/replay robustness); immediate accepts need 2 consecutive strong frames (weak frame disarms). Contract change: acquisition is now >=2 agreeing frames (~1s at 0.5s hop); legacy one-shot tests updated to two-frame semantics with explanatory docstrings.
**M3:** is_speech_class() casefold/substring helper; router directed rule + active_speaker now work on real PANNs labels.
**Tests:** 46 passed (demotion, 2-frame confirm, hot-apply, speech-class). Serve restarted on new code.
**Live checks pending (one guided run covers steps 2-5):** recalibrate with TV in world phase -> impostor_mean>=0.18+h0 label; Alan talks -> active_speaker==Alan; TV takeover -> demote <=10s; 60s TV-only -> zero MATCHED.

## 19:32 — [Step 6 landed] the copies incident, closed

**Live incident:** viewscreen showed duplicate identities ("you" + "Alan"). Store held FOUR prints: Alan, you.json (18:36 — M11 default-name calibration), testperson (15:55 — re-leaked: test fixture daemon threads outlive the env isolation), Emerson. Both duplicates quarantined.
**Also observed live: the revocable hold WORKING** — two clean SPRT revoke lines at the designed bound (-8.2 = 2B), identity demoting instead of latching forever.
**Fixes:** LocalStore casefolds filename keys (ALAN==alan==Alan, one speaker); calibration refuses name "you"/empty unless exactly one enrolled (then reuses it); synthetic demo serve isolated to <data_dir>/demo/; silence unenrollable (VAD guard in enroll — the no-voiced-windows fallback was the silence-print path).
**Tests:** 49 passed incl. new test_store_hygiene.py (silence refusal, casefold dedupe, demo isolation). Serve restarted on step-6 code. Store now: Alan + Emerson only.

## 19:58 — [Step 7 + training UI landed]

**M5:** censored rolling H0 window (300 frames): decided-target scores censored by flag, plausibly-target scores hard-censored (target_mean - 2sigma cut — one owner pre-decision frame otherwise inverts the test; caught by suite), top decile dropped, honest floors. Background now learns what live voices score.
**M6:** AS-Norm cohort engages at 1 other enrollment (impostor-model blended).
**M7:** held identity emitted on coasting tracks (manager.held_name) — no more UNKNOWN between utterances; HUD dims held cards.
**M9 slice:** STT focus-gated temporally — only the focused source's speaking windows feed the transcriber; everything else stays tracked.
**Train-by-marking:** per-source 15s audio buffers; POST /enroll_track {id,name}; left panel now holds ALL speech sources (unknowns get a "name this voice" button; VAD guard + name rules apply).
**Tests 49 green.** serve8 restarted with everything.

## 20:18 — [Fix] duplicate identity claims (the "not stitching" report)

**Live evidence:** map carried TWO sources both named Alan — one Speech, one a leftover White-noise track holding the name via the M7 held emission.
**Fix:** _dedupe_identity_claims in pipeline (one live track per identity; live speech outranks held/stale claims; losers forget_track and re-decide honestly). Unit test pins it. 50 green, serve9 up.

## 20:25 — [Carry-forward] identified unknowns keep their data

**What:** router now emits a stable `track` key on every source + identity.name/held passthrough (also fixes the client's dead focus-exemption clause). Audio buffers, UI cards, and enroll_track all key on track — an UNKNOWN becoming "Alan" keeps its card, waveform, buffered speech, and focus instead of orphaning them.
**Tests 50 green; serve10 up.**

## 20:43 — [HANDOFF to Opus 4.6] session verdict + state

**Verdict (Alan):** Fable 5 judged unfit for expert pair-work after repeated overlooked context (asked for symptoms he had already reported; earlier fabrications on record in this chronicle + alan-digital-core chronicle 2026-07-01). Model switching back to claude-opus-4-6.

**State for the next session — trust the disk, verify before building:**
- Job voice-discriminator-real: DEACTIVATED at Alan's stop order; step 1 popped (B3 soak PASSED: 10min at 118-120 win/60s, 26ms snapshots). Steps 2-7 CODE LANDED + unit-tested (50 green) but live checks NOT observed: step-2 recalibration w/ TV world phase, step-4 TV-demote, step-5 active_speaker.
- Observed working live: revocable hold (2x SPRT revoke at -8.2), Chloe train-by-marking matching at cos 0.53-0.66 llr 13.66 (serve10.log 20:34) — but Alan reported the SCREEN not reflecting it; UI-vs-engine display gap UNDIAGNOSED. UI symptom thread: copies -> not stitching -> she not on left -> named 6 clips not working.
- Uncommitted: buffer overlap fix (append hop tail only — buffers doubled: 90s from 45s wall), carry-forward (router track key), dedupe claims, steps 3-7, UI panel/train button.
- All daredevil processes stopped, mic off, port 8770 free.
**Next session first moves:** read this + docs/GAP_VOICE_DISCRIMINATOR.md + state/jobs/voice-discriminator-real/manifest.md; diagnose the UI display gap AGAINST WHAT ALAN ALREADY REPORTED (in this chronicle) before touching code.
